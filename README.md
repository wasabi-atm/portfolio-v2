# portfolio-v2

Tailwind CSS usage

- Install deps: `npm install`
- Build CSS once: `npm run build:css`
- Watch CSS during development: `npm run watch:css`

Notes

- We keep the Tailwind CDN snippet in HTML for easy preview while developing.
- Compiled CSS is written to `dist/output.css` and is also linked from the pages.

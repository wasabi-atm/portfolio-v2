{
  "name": "carte-site",
  "version": "1.0.0",
  "description": "Standalone Carte landing page",
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o dist/output.css",
    "watch:css": "tailwindcss -i src/input.css -o dist/output.css --watch",
    "dev": "npx http-server ."
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.12"
  }
}
